<table class="table">
	<thead>
		<tr>
		<th>ジャンル検索</th>
		</tr>
	</thead>
	<tbody>
		<tr>
			<td><%= link_to "すべての商品", products_path %></td>
		</tr>
		<% genres.each do |genre| %>
			<tr>
				<td><%= link_to genre.name, products_path(genre_id: genre.id) %></td>
			</tr>
		<% end %>
	</tbody>
</table>



